name: Docker Image

on:
  push:
    tags:
      - v*

jobs:
  build-and-push:
    runs-on: codeberg-small
    steps:
      - uses: actions/checkout@v4

      - name: Install buildah
        run: |
          apt-get update -qq
          apt-get install -y -qq buildah ca-certificates

      - name: Login to Codeberg Container Registry
        run: |
          buildah login -u "${{ github.repository_owner }}" \
            -p "${{ secrets.GITHUB_TOKEN }}" \
            codeberg.org

      - name: Build and tag
        run: |
          REGISTRY="codeberg.org/${{ github.repository }}"
          VERSION="${GITHUB_REF_NAME#v}"
          MAJOR_MINOR="${VERSION%.*}"
          buildah build -t "${REGISTRY}:${VERSION}" .
          buildah tag "${REGISTRY}:${VERSION}" "${REGISTRY}:${MAJOR_MINOR}"
          buildah tag "${REGISTRY}:${VERSION}" "${REGISTRY}:latest"

      - name: Push to registry
        run: |
          REGISTRY="codeberg.org/${{ github.repository }}"
          VERSION="${GITHUB_REF_NAME#v}"
          MAJOR_MINOR="${VERSION%.*}"
          buildah push "${REGISTRY}:${VERSION}"
          buildah push "${REGISTRY}:${MAJOR_MINOR}"
          buildah push "${REGISTRY}:latest"
